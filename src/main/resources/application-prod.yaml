spring:
  jpa:
    hibernate:
      ddl-auto:
        update
  datasource:
    url:
      jdbc:mysql://localhost:3306/healdb
    username:
      root
    password:
      use_password_from_aws_paramstore
    driver-class-name:
      com.mysql.cj.jdbc.Driver